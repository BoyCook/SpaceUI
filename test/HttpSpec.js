var should = require('should');
var fs = require('fs');
var vm = require('vm');
var HTTP = require('../assets/http.js').HTTP;

describe('HTTP', function () {
	
    before(function() {
    	// vm.runInThisContext(fs.readFileSync('assets/'));
    });

    describe('#HTTP', function () {
        it('should work', function () {

        });
    });
});
